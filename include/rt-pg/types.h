#pragma once

#include <bitset>
#include <vector>

#include "rt-pg/property.h"
#include "uuid.h"

namespace rtpg {
using UUID = uuids::uuid;
using Properties = std::vector<Property>;
}  // namespace rtpg
